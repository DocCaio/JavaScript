Pront de comando 

node -v  verifica versão do node

mkdir + nome  cria pasta

cd + nome  para entar napasta

code .  para abirir o vs code

npm init -y  criar projeto 

npm start para testar no terminal 



:root {
    --variaveis: ;
}

@media screen and (prefers-color-scheme: dark) {
    :root{
        --variaveis: ;
    }
}
 
Você declara as variáveis dentro de :root, não é?
O que ficar dentro do media query vai ser aplicado quando o usuário colocar o sistema no modo escuro